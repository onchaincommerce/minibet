"use client";

export default function CasinoBackground() {
  return (
    <div className="fixed inset-0 w-full h-full overflow-hidden z-[-1] bg-black pointer-events-none">
      {/* Background with no falling symbols */}
    </div>
  );
} 